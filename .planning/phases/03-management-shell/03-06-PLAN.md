---
phase: 03-management-shell
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified: []
autonomous: false
requirements: [SQD-01, SQD-02, SQD-03, SQD-04, SQD-05, SQD-06]

must_haves:
  truths:
    - "The squad screen shows all 16 players with name, position, age, height, 10 attribute bars, and fitness"
    - "The manager selects a starting 11 and bench 5 on the squad screen before kicking off"
    - "The selected squad takes the pitch — match uses season squad, not the old test archetypes"
    - "After a match, all 19 AI fixtures resolve and the table updates before Hub is shown"
    - "The fixtures screen shows 38 matchdays with the player team's fixtures highlighted"
    - "The table screen shows 20 teams sorted correctly by points, goal difference, goals for"
    - "After matchday 38, a champion is declared on the Hub screen"
    - "Player names are procedurally generated (not blank or placeholder)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 management shell. The manager plays through at minimum 2 matchdays end-to-end and confirms all six success criteria are observable.

Purpose: Automated tests confirm logic correctness. This checkpoint confirms the UX is navigable and the full season loop works from a human perspective.
Output: Approved or issues reported for gap closure.
</objective>

<execution_context>
@C:/Users/nickj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nickj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-management-shell/03-CONTEXT.md
@.planning/phases/03-management-shell/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated checks</name>
  <files>none</files>
  <action>
    Run the full test suite and build to confirm everything is green before asking the user to verify:
    1. npx vitest run — all tests pass
    2. npx vite build — build succeeds with no TypeScript errors

    Report any failures and fix them before proceeding to Task 2.
  </action>
  <verify>
    <automated>npx vitest run && npx vite build</automated>
  </verify>
  <done>All tests pass and build succeeds with no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete management shell</name>
  <files>none</files>
  <action>
    Human verification of the Phase 3 management shell. Run npx vite dev and open http://localhost:5173

    What was built: Complete management shell — 20-team season with quality tiers (4 strong / 10 mid / 6 weak), Hub screen (position / next fixture / last result), Squad screen (16 players with name, position, age, height, attribute bars, fitness, starting XI toggle, Kick Off validation), Fixtures screen (38 matchdays), Table screen (20 teams sorted by pts/GD/GF), AI quick-sim batch after each match, fatigue persistence between matches, champion declaration at season end, and procedurally generated player names throughout.

    Verification steps:

    Screen navigation (30 seconds):
    1. App opens on Hub screen — confirm: team name visible, season number, league position, next fixture shown
    2. Click Squad tab — confirm: 16 players listed with name, position, age (e.g. "24"), height (e.g. "181cm"), attribute bars visible, fitness bar visible. IMPORTANT: personality traits must NOT appear on this screen.
    3. Click Fixtures tab — confirm: 38 matchdays visible (scroll), player team fixtures highlighted, unplayed show "vs"
    4. Click Table tab — confirm: 20 teams listed with Pos/P/W/D/L/GF/GA/GD/Pts columns, player team highlighted

    Squad selection validation (1 minute):
    5. On Squad screen, click Kick Off before setting a lineup — confirm: button disabled or inline warning shown
    6. Set valid starting XI (11 starters including 1 GK) and 5 bench — confirm: Kick Off becomes enabled
    7. Remove the GK from starters — confirm: warning appears saying GK is required

    First matchday (3 minutes):
    8. Set valid lineup, click Kick Off — confirm: match starts, player names are procedural (not "home-1" or "player-team-player-0")
    9. Play to full-time (use speed slider to accelerate)
    10. Full-time overlay — confirm: "Continue" button visible (not "Click anywhere to close")
    11. Click Continue — brief "Simulating..." then Hub shown
    12. Go to Table — confirm: 20 teams show results (played > 0 for multiple teams)
    13. Go to Fixtures — confirm: matchday 1 shows scores, matchday 2+ shows "vs"

    Second matchday (2 minutes):
    14. On Squad screen, check fitness bars — players who played show reduced fitness (not 100%)
    15. Select a different lineup to rotate, kick off matchday 2
    16. Play to full-time, continue — matchday 2 results visible, table updates again

    Resume signal: Type "approved" if all checks pass, or describe specific issues found.
  </action>
  <verify>
    <automated>npx vite build</automated>
  </verify>
  <done>Human confirms: all 4 screens navigate correctly, squad selection validation works, selected squad takes pitch, AI sims run after full-time, fatigue carries between matches, player names are procedurally generated</done>
</task>

</tasks>

<verification>
All six phase success criteria verified by human:
1. Squad screen shows players with attributes, age, fitness (SQD-01)
2. Manager selects starting XI + bench; selected squad takes pitch (SQD-02)
3. Fixture list shows all 38 matchdays (SQD-03)
4. Table updates correctly after each matchday (SQD-04)
5. Champion declared at season end; squad carries to new season (SQD-05)
6. Player names are procedurally generated (SQD-06)
</verification>

<success_criteria>
Human approves all verification steps. Phase 3 complete.
</success_criteria>

<output>
After approval, create `.planning/phases/03-management-shell/03-06-SUMMARY.md` and update STATE.md to mark Phase 3 complete.
</output>
