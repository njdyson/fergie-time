---
phase: 05-server-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - vite.config.ts
  - package.json
autonomous: false
requirements:
  - SERV-02

must_haves:
  truths:
    - "Vite proxies /api/* requests to Express server during development"
    - "Both servers can run simultaneously with a single npm command"
    - "curl to Vite port for /api/health returns the Express response, not Vite HTML"
  artifacts:
    - path: "vite.config.ts"
      provides: "Proxy configuration for /api/* to localhost:3001"
      contains: "proxy"
  key_links:
    - from: "vite.config.ts"
      to: "server/index.ts"
      via: "proxy target http://localhost:3001"
      pattern: "localhost:3001"
---

<objective>
Configure Vite dev proxy to forward /api/* requests to the Express server, and set up a combined dev script so both servers run with one command.

Purpose: Developers need a single command to start the full stack. The proxy ensures the client can call /api/* endpoints without CORS issues during development.
Output: Updated vite.config.ts with proxy, combined dev script in package.json
</objective>

<execution_context>
@C:/Users/nickj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nickj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-server-foundation/05-RESEARCH.md
@vite.config.ts

<interfaces>
From vite.config.ts (current):
```typescript
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    target: 'es2022',
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite proxy config and combined dev script</name>
  <files>vite.config.ts, package.json</files>
  <action>
    Update vite.config.ts to add server.proxy configuration:
    - Proxy /api to http://localhost:3001
    - Set changeOrigin: true
    - No rewrite needed -- Express routes already include /api prefix

    ```typescript
    server: {
      proxy: {
        '/api': {
          target: 'http://localhost:3001',
          changeOrigin: true,
        },
      },
    },
    ```

    Update package.json scripts:
    - Install concurrently as dev dependency: `npm install -D concurrently`
    - Add "dev:client": "vite"
    - Update "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\""
    - Keep existing "dev:server": "tsx watch server/index.ts" (added in Plan 02)

    This gives cross-platform support (Windows + Linux) for running both servers.
  </action>
  <verify>
    <automated>npx tsx server/index.ts &amp; sleep 2 &amp;&amp; curl -s http://localhost:3001/api/health &amp;&amp; kill %1</automated>
  </verify>
  <done>vite.config.ts has proxy config for /api/*, package.json has combined dev script using concurrently</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full dev stack end-to-end</name>
  <action>
    Human verifies the complete Phase 5 deliverable:

    1. Run `npm run dev` -- both servers should start (Express on 3001, Vite on 5173)
    2. Open http://localhost:5173 -- the existing game should load normally
    3. Open http://localhost:5173/api/health in a new tab -- should show JSON: { "status": "ok", "timestamp": "..." }
    4. Check that data/fergie-time.db was created in the project root
    5. Run `npx vitest run server/` -- serialization tests should all pass
    6. Stop the servers (Ctrl+C)
  </action>
  <verify>Human confirms all 6 checks pass</verify>
  <done>Full dev stack verified: Express + SQLite + Vite proxy + serialization all working</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts both Express and Vite servers
2. http://localhost:5173/api/health returns Express JSON response (not Vite HTML)
3. Existing game still works at http://localhost:5173
4. All server tests pass: `npx vitest run server/`
</verification>

<success_criteria>
- Vite proxies /api/* to Express during development
- Combined dev script starts both servers with one command
- Existing client functionality unaffected
- Human verifies the full stack works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-server-foundation/05-03-SUMMARY.md`
</output>
