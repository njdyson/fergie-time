---
phase: 07-squads-names
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/season/season.ts
  - src/ui/screens/squadScreen.ts
  - src/main.ts
autonomous: false
requirements: [SQD2-03, SQD2-04, NAME-01, NAME-02, NAME-03]

must_haves:
  truths:
    - "Squad screen shows 25 players with positions, attributes, and shirt numbers"
    - "Clicking a shirt number opens an inline edit input; entering a valid unique number updates the player's shirtNumber"
    - "Shirt number edit rejects duplicates within the team and non-numeric or out-of-range values"
    - "Matchday selection allows 11 starters + 7 subs from 25-man roster"
    - "Squad screen shows Bench: N/7 counter (not N/5)"
    - "New game creation fetches names from randomuser.me and passes them to createSeason"
    - "If randomuser.me is unreachable during game creation, game still creates with fallback names"
    - "New season (startNewSeason) also uses 25-player squads with names"
    - "Opponent bench in player match uses slice(11, 18) not slice(11, 16)"
  artifacts:
    - path: "src/ui/screens/squadScreen.ts"
      provides: "25-player squad display with 11+7 selection, shirt numbers, and inline shirt number editing"
      contains: "Bench.*7"
    - path: "src/main.ts"
      provides: "Async name fetch wired into game creation flow"
      contains: "getNames"
    - path: "src/season/season.ts"
      provides: "createSeason accepts names parameter, startNewSeason uses 25 players"
      contains: "names"
  key_links:
    - from: "src/main.ts"
      to: "src/season/nameService.ts"
      via: "import getNames, call before createSeason"
      pattern: "getNames"
    - from: "src/main.ts"
      to: "src/season/season.ts"
      via: "createSeason with names parameter"
      pattern: "createSeason"
    - from: "src/ui/screens/squadScreen.ts"
      to: "src/simulation/types.ts"
      via: "PlayerState.shirtNumber rendering and editing"
      pattern: "shirtNumber"
---

<objective>
Wire name fetching and 25-man squads into the UI and game creation flow, with editable shirt numbers.

Purpose: Connect the data layer (Plan 01) to the game creation flow and squad screen so players see 25-man rosters with realistic names and can select 11+7 matchday squads. Shirt numbers are editable per SQD2-03.
Output: Working end-to-end flow where new game fetches names, creates 25-player teams, squad screen shows all 25 with editable shirt numbers, and matchday selection is 11+7.
</objective>

<execution_context>
@C:/Users/nickj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nickj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-squads-names/07-RESEARCH.md
@.planning/phases/07-squads-names/07-01-SUMMARY.md

@src/season/season.ts
@src/ui/screens/squadScreen.ts
@src/main.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/season/nameService.ts (created in Plan 01):
```typescript
export interface NameEntry { first: string; last: string; nationality: string; }
export async function fetchNames(count: number, natCode: string): Promise<NameEntry[]>;
export async function getNames(count: number, rng: () => number): Promise<string[]>;
```

From src/season/teamGen.ts (updated in Plan 01):
```typescript
export function createAITeam(tier: TeamTier, teamId: string, _teamName: string, rng: () => number, names?: string[]): PlayerState[];
// Now produces 25 players with shirtNumber
```

From src/season/season.ts (updated in Plan 01):
```typescript
export function validateSquadSelection(selection: SquadSelection): { valid: boolean; reason?: string };
// Now expects bench.length === 7
```

From src/simulation/types.ts (updated in Plan 01):
```typescript
// PlayerState now includes:
readonly shirtNumber?: number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire name fetch into game creation, update season.ts and squadScreen.ts with editable shirt numbers</name>
  <files>src/season/season.ts, src/ui/screens/squadScreen.ts, src/main.ts</files>
  <action>
**src/season/season.ts** -- Update createSeason and startNewSeason:

1. Add optional `names?: string[]` parameter to `createSeason` (after `seed`). Pass a slice of names to each `createAITeam` call: `names?.slice(index * 25, (index + 1) * 25)`. The player team squad should also receive names from this pool (the first 25 names). Since the player team's squad is passed in already created, the names need to be applied at creation time in main.ts instead.
   - Actually: createSeason receives `playerSquad` already built. For AI teams, pass name slices to createAITeam. Each AI team needs 25 names, so the names array should have at least 19*25=475 names for AI teams. Pass `names?.slice(aiIndex * 25, (aiIndex + 1) * 25)` to each createAITeam call.

2. Update `startNewSeason`: Currently regenerates AI teams with `createAITeam(tier, team.id, team.name, rng)`. Add no names parameter here -- new seasons use fallback procedural names (acceptable since names are cosmetic and fetching API at season rollover adds complexity for little benefit). Leave startNewSeason synchronous.

**src/main.ts** -- Update game creation flow:

1. Import `getNames` from `./season/nameService.ts`.
2. Find the new game creation code (the `if (isNewGame)` block). Currently:
   ```
   const initialRosters = createMatchRosters();
   const playerSquad = [...initialRosters.home, ...initialRosters.homeBench];
   seasonState = createSeason('player-team', teamName, playerSquad, 'season-1');
   ```
3. Replace with async flow:
   - Fetch names: `const names = await getNames(500, seedrandom('names-' + teamName));` (500 = 20 teams x 25 players)
   - Create player squad using createAITeam: `const playerSquad = createAITeam('mid', 'player-team', teamName, seedrandom('player-' + teamName), names.slice(0, 25));`
   - Create season with remaining names: `seasonState = createSeason('player-team', teamName, playerSquad, 'season-1', names.slice(25));`
   - This replaces the createMatchRosters() approach for new game creation. The player team now gets 25 players via createAITeam with 'mid' tier (per research recommendation).

4. Find the opponent bench setup: Change `opponentSquad.slice(11, 16)` to `opponentSquad.slice(11, 18)`.

5. Make the containing function async if not already. The login/boot flow that calls this code likely needs the function to be async or use `.then()`. Check the call site and update accordingly.

**src/ui/screens/squadScreen.ts** -- Update for 25 players, 7-man bench, and editable shirt numbers:

1. Update file header comment: "Shows all 25 players" (was 16).
2. Update default selection logic: Change `else if (i < 16)` to `else if (i < 18)` so first 11 are starters, next 7 (indices 11-17) are bench, rest are reserves (unselected).
3. Update bench counter display: Change `Bench: ${benchCount}/5` to `Bench: ${benchCount}/7`.
4. Display shirt number from `p.shirtNumber ?? ''` (not index-based `i + 1`) in the # column. Style it as a narrow fixed-width column (30px, right-aligned, dimmed color).
5. Ensure the table still fits. With 25 rows, scrolling should work via the existing `overflowY: 'auto'` on the container.

6. **Editable shirt numbers (SQD2-03):**
   - Add a private `shirtNumbers: Map<string, number>` field to SquadScreen, initialized from `p.shirtNumber` values in `setPlayers`.
   - Make the shirt number cell clickable (separate click handler from the row toggle -- use `e.stopPropagation()`). On click, replace the number text with an `<input type="number" min="1" max="99">` element, pre-filled with the current number, styled inline (same font, small width ~40px, dark background matching row).
   - On input blur or Enter key: validate the new number:
     a. Must be an integer between 1 and 99.
     b. Must not be already assigned to another player in the same team (check `this.shirtNumbers` map, excluding current player).
     c. If invalid, flash the input border red briefly and revert to previous number.
     d. If valid, update `this.shirtNumbers.set(playerId, newNumber)`, also update the player object: find player in `this.players` by id, create a shallow copy with the new shirtNumber and replace in the array (PlayerState is readonly, so spread `{...player, shirtNumber: newNum}`). Re-render.
   - On Escape key: cancel edit, revert to display mode without changes.
   - Add a public `getUpdatedPlayers(): PlayerState[]` method that returns the current players array with any shirt number edits applied. This allows main.ts to read back edited squads for persistence.
   - Add an `onShirtNumberChange` callback registration method (same pattern as `onSelectionChange`) so consumers can react to edits (e.g., mark season state dirty for auto-save).
  </action>
  <verify>
    <automated>npx vitest run src/season/ -x</automated>
  </verify>
  <done>New game creation fetches 500 names, creates player team with createAITeam('mid'), passes remaining names to createSeason for AI teams. Squad screen shows 25 players with shirt numbers that are click-to-edit with uniqueness validation. Default selection is 11+7. Bench counter shows N/7. Opponent bench uses slice(11, 18). All season tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 25-man squads with realistic names and editable shirt numbers end-to-end</name>
  <files>src/ui/screens/squadScreen.ts, src/main.ts</files>
  <action>Human verifies the complete squad expansion, name generation, and shirt number editing flow end-to-end.</action>
  <what-built>
    Complete squad expansion from 16 to 25 players with realistic nationality-weighted names, 18-man matchday selection (11+7), editable shirt numbers, and shirt number uniqueness validation on the squad screen.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the dev server
    2. Open the app in browser, create a new game with any team name
    3. On the squad screen, verify:
       - Exactly 25 players are listed (not 16)
       - Each player has a shirt number (1-25) shown
       - Player names look realistic and nationality-appropriate (not "James Smith" style procedural names)
       - Default selection shows 11 starters + 7 bench (check the counter shows "Starters: 11/11 | Bench: 7/7")
       - Remaining 7 players are unselected reserves
    4. **Test shirt number editing:**
       - Click on a player's shirt number -- it should become an editable input field
       - Change the number to a valid unique number (e.g., 30) and press Enter -- it should update
       - Try changing a number to one already in use by another player -- it should reject (flash red, revert)
       - Try entering 0 or 100 -- it should reject (out of 1-99 range)
       - Press Escape during edit -- it should cancel without changes
       - Verify clicking the shirt number does NOT toggle the player's starter/bench/out status
    5. Select a valid 11+7 squad and click "Kick Off"
    6. Play or skip through a match, verify it completes normally
    7. After the match, advance to next matchday -- verify AI results appear (AI teams are also using 25-man squads)
    8. Check the league table updates correctly after matchday
    9. If possible, disconnect internet and create another new game -- verify it still creates with fallback names (names will be from the procedural pool, less realistic but functional)
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All phase success criteria confirmed: 25 players visible, 11+7 selection works, names look realistic, fallback works offline, shirt numbers visible and editable with uniqueness validation.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. npx vitest run (full suite green)
2. Manual: new game shows 25 players with realistic names, 11+7 selection, shirt numbers
3. Manual: shirt numbers are click-to-edit with uniqueness validation (duplicate rejected, out-of-range rejected, Escape cancels)
4. Manual: full matchday cycle works (play match, AI sims, table updates)
</verification>

<success_criteria>
- Squad screen shows 25 players with editable shirt numbers for all teams
- Shirt number click-to-edit works: valid unique numbers accepted, duplicates and out-of-range rejected
- Matchday selection is 11 starters + 7 subs (Bench: N/7)
- Player names look realistic (from randomuser.me) for new games
- Fallback to procedural names works when API unreachable
- Full matchday cycle (play + AI sim + table update) works with 25-man squads
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-squads-names/07-02-SUMMARY.md`
</output>
